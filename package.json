{
  "name": "tomislav-martinez-portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "preview": "npm run build && node scripts/preview.mjs"
  }
}
